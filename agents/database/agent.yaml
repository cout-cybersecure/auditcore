name: database
version: 0.1.0
purpose: |
  Interpret database evidence: slow queries, indexes, connection pressure,
  replication lag, cache hit ratios, storage bottlenecks. Recommend
  tuning + architectural improvements.

task_kind: REASON
budget_hint: normal
privacy: standard

inputs:
  - run_id
  - asset_ids: list[UUID]
  - evidence_ids: list[UUID]   # filtered to category=database

output_schema: ../_shared/domain_agent_output.json
system_prompt: prompts/system.md
